{
  "type": "object",
  "properties": {
    "commitments": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "act_type": {
            "type": "string",
            "enum": ["offer", "commit", "confirm", "revoke", "renegotiate", "quote", "approve", "escalate", "query"]
          },
          "deliverable_en": { "type": "string" },
          "deliverable_original": { "type": "string" },
          "due_at": { "type": ["string", "null"] },
          "amount": { "type": ["number", "null"] },
          "currency": { "type": ["string", "null"] },
          "price_changed": { "type": ["boolean", "null"] },
          "counterparty_name": { "type": ["string", "null"] },
          "relates_to": {
            "type": ["string", "null"],
            "description": "Reference (e.g. \"C1\") of the already-logged commitment this message is about, from the ALREADY LOGGED list. Null only when this is a genuinely new commitment. Narrowed to an enum of the refs actually supplied at call time — app/ledger/schema.py's build_extraction_json_schema."
          },
          "evidence_span": { "type": "string" },
          "confidence": { "type": "number" }
        },
        "required": ["act_type", "deliverable_en", "deliverable_original", "evidence_span", "confidence"],
        "additionalProperties": false
      }
    }
  },
  "required": ["commitments"],
  "additionalProperties": false
}
